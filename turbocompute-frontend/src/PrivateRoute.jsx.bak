import React from "react";

/**
 * Very simple PrivateRoute stub.
 * Replace auth logic as needed.
 */
export default function PrivateRoute({ children }) {
  // For now allow everything. Later check token/auth here.
  return children;
}
