import React from "react";
import { Typography } from "@mui/material";

export default function Home() {
  return (
    <div>
      <Typography variant="h3">TurboCompute — Frontend</Typography>
      <Typography>Welcome — Home placeholder page.</Typography>
    </div>
  );
}
