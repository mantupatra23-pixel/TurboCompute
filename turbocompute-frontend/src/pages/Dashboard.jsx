import React from "react";
import { Typography } from "@mui/material";

export default function Dashboard() {
  return (
    <div>
      <Typography variant="h3">Dashboard</Typography>
      <Typography>Dashboard placeholder.</Typography>
    </div>
  );
}
